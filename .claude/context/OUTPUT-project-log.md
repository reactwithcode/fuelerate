[Do not write this manually. AI maintains this file automatically after every meaningful action.]

This is the project's persistent memory. AI reads it at the start of every session to restore context. It will contain:

- **Timestamped entries** — What was done and when
- **Decisions made** — What was decided and why, including alternatives that were considered
- **Architectural choices** — Technical decisions that affect the project long-term
- **Risks & open questions** — Things to watch out for in future development

## 2026-08-29 — Comparison Table: fixed Add to Cart button (2 bugs) — now adds the current product's variant

**Bug 1 — button never wired up**: the CTA button (`.ct__cta`) was outside the `<comparison-table>` custom element's closing tag in `sections/section-comparison-table.liquid`, while `comparison-table.js`'s `connectedCallback` does `this.querySelector('.ct__btn[data-variant-id]')` — scoped to the element itself, so `ctaBtn` was always `null` and no click listener was ever attached. **Fix**: moved the `.ct__cta` block inside `<comparison-table>...</comparison-table>`, right after the pagination dots.

**Bug 2 (design change, requested by user)**: the button was hardcoded to add a specific product via a `button_product` picker (set to `terra-therapy-fitted-grounding-bedsheet` on the product template instance) — fragile, since this section sits on the product page (`templates/product.json`) and should reflect whatever product the customer is actually viewing. **Fix**: button now uses `product.selected_or_first_available_variant` when a `product` object is in context (i.e. on a product page). The `button_product` picker is kept only as a fallback for when the section is used on a non-product template — the section has an unconfigured preset so it's available everywhere per our "usable everywhere" convention. Updated the picker's schema label/info to make this fallback role explicit.

- **Known limitation, not addressed**: the button renders server-side with whatever variant is selected/first-available at page load. If the customer changes variant via the product's own variant selector without a full page reload, this button won't update — no event listener wired to the theme's variant-change mechanism. Flagged but not fixed, since no existing variant-change custom event was found in the theme analysis and wiring one up would mean touching base product-form JS, which we avoid without discussion. Revisit if the client reports the button adding the wrong variant after a selector change.
- **Gotcha this session**: `shopify theme dev` was running in watch mode against `developmet-test.myshopify.com`, and it silently reverted the first attempt at this exact fix back to the pre-fix file content within the same session (confirmed via `git status`/`git diff` showing a clean tree matching HEAD, i.e. the edit was never committed and got wiped from disk) — same symptom already logged once before for a CSS edit on this file (see below, "silently reverted on disk" note). Re-applied and verified via `grep` after a pause; it stuck the second time. **If edits to this file seem to silently disappear, check `ps aux | grep theme dev` and verify changes on disk directly, not just the last Read in context** — don't assume the edit tool failed.

**Bug 3 — found after user tested live**: cart total updated correctly (item really added), but the drawer opened showing a blank panel (only the checkout/estimated-total footer visible, no line items). Root cause: `CartDrawer.renderContents()` (`assets/cart-drawer.js:79-96`) only clears the `is-empty` class from `.drawer__inner`, not from the outer `<cart-drawer>` element itself. The normal Add to Cart flow (`assets/product-form.js:115`, inside its `.finally()`) separately clears `is-empty` from the outer `cart-drawer` element after calling `renderContents()` — `comparison-table.js` was missing that second step. `component-cart-drawer.css:47-56` keys off the outer `cart-drawer.is-empty` class to `display:none` the `.drawer__header` and force the empty-state grid layout, which is exactly what was hiding the newly-rendered items.
- **Fix**: `assets/comparison-table.js` now calls `cartDrawer.classList.remove('is-empty')` right after `cartDrawer.renderContents(cartData)`.
- **Takeaway**: `CartDrawer.renderContents()` is not a self-contained "add to cart and show it" call — every caller is expected to also manage the outer `is-empty` class itself. Worth remembering for any future component that adds to cart outside the standard `product-form`/`quick-add` components.
- Not yet re-verified live after this third fix — waiting on user to retest.

## 2026-08-29 — Trust Bar: switched label font to Recoleta, bundled the 4 Figma icons as position-based defaults

User asked to fetch the Trust Bar's Figma frames (desktop node 1:1611, mobile node 1:3426) and update `sections/section-trust-bar.liquid` to use Recoleta font and the Figma icons as defaults.
- **Layout/spacing was already accurate**: comparing `get_design_context` output against the existing file, the row/column flip at 749px, 8px icon-label gap, 5px mobile item gap, 19px mobile side padding, 36px icon size, and 20px desktop / 16px mobile label size all already matched Figma exactly — no structural changes made. Left the `padding_top`/`padding_bottom` 0.5 mobile-multiplier formula untouched (pre-existing convention shared with the CPI section) even though it puts default mobile padding at 14px vs. Figma's 23px — a range-setting default, not a hard requirement, and changing the shared formula wasn't asked for. Worth a look if a merchant later flags mobile spacing as too tight.
- **Font**: removed the section's own Google Fonts `Figtree` `<link>` (was only used by `.trust-bar__label` in this file) and replaced it with the same self-hosted Recoleta `@font-face` (Regular 400 only, since that's the only weight used here) already used by the CPI/comparison-table/interactive-benefit-showcase sections, pointed at the same Shopify CDN file. `.trust-bar__label` now uses `font-family: 'Recoleta', serif`.
- **Icons**: downloaded the 4 Figma icons (ShieldCheck, USA flag, Package, Tree — all `fill="white"`, 36×36) via `download_assets`, stripped Figma's leaked full-canvas background rects from the raw export, and saved as `assets/trust-bar-icon-{shield-check,usa,package,tree}.svg`. Wired them into `sections/section-trust-bar.liquid` using the **same position-based fallback pattern the CPI section already uses** for its benefit icons (`default_icons[forloop.index0]`, rendered only when `block.settings.icon` is blank) — reused an existing codebase convention rather than inventing a new schema field. Updated the `icon` image_picker's label/info to "Custom icon" / "Optional. Defaults to a preset icon based on position (1–4)." to match CPI's wording. The bundled preset's 4 default blocks (Guarantee / USA / Shipping / Tree) already were in the matching order, so no preset changes needed.
- **Not yet verified in a live theme preview** — no dev server running this session.

## 2026-08-29 — Comparison Table: reverted the "shorter chip" design — attribute chip now fills the full column height on mobile

After a couple of rounds trying to nail the exact white-space behavior around the "how it works" chip (color, then sides, then top/bottom), the user settled on: no white gap anywhere — the chip should fill the entire column height, matching the Solution chip exactly. This reverses the "shorter chip, centered, matching Figma's tighter mockup" design from several rounds back.
- `assets/section-comparison-table.css`: `.ct__row--ours .ct__col-track` changed `align-items: center` → `align-items: stretch`. `.ct__row--ours .ct__col-track .ct__cell` gained an explicit `height: 100%` — needed because an externally-added unscoped rule (not mine, found a couple rounds ago and left alone at the time — see previous entry) sets a fixed `height: 90px` on the same selector for desktop; without overriding it, mobile would inherit that 90px cap even with `align-items: stretch` on the parent.
- **Takeaway for future rounds**: the "shorter chip with visible white space" design is no longer wanted — don't reintroduce `align-items: center`/`flex-start` + shrink-to-content here without checking first, since this has flip-flopped a few times this session.

## 2026-08-29 — Comparison Table: attribute chip now bleeds past the card edge on mobile too, by fixing the slider's shared-width assumption

User asked for the "how it works" chip to bleed outside its column like the Solution chip does (currently flush/contained, since last round removed the viewport's bleed margin to fix the slide-drift bug). Rather than just re-add the margin (which would reintroduce that bug), fixed the actual root cause in `assets/comparison-table.js`: `updateOffset()` measured width from `this.firstViewport` (header row's) and applied that one shared value to every row's track. Changed it to measure each track's own parent viewport (`track.parentElement.offsetWidth`) independently — so a row's viewport can now be a different width (e.g. bled past the card edge) without any drift, since each row's slide offset is entirely self-contained.
- With that fixed, restored `margin: 0 -8px` on `.ct__row--ours .ct__col-viewport` in `assets/section-comparison-table.css`, matching the Solution cell's bleed.
- **Note**: hit a strange one mid-fix — the first attempt at this CSS edit applied cleanly but then silently reverted on disk within ~2 seconds (verified via direct `grep`, not just a stale curl cache) before the dev server ever picked it up. Re-applied it and it stuck. Watch for this if edits to this file seem to "not take" — verify against the file on disk directly, not just the last Read in context.
- Verified both the JS and CSS changes live via the running `theme dev` server.

## 2026-08-29 — Comparison Table: two real bugs — merchant-uploaded row image overriding mobile, and a growing slider-drift glitch on slide 2+

User sent DevTools screenshots that revealed the actual root cause of most of this session's back-and-forth: `section.settings.our_row_bg` **is set** on this section instance (a real uploaded file, `..._Therapy_row_background.png`), which was overriding the tuned CSS gradient on `.ct__cell--solution` via `background-size: cover` this whole time on mobile. Also surfaced a genuinely new bug: swiping to slide 2/3/4 ("Cost over time", "What to consider") showed a thin vertical strip that grew wider with each swipe.

**Bug 1 — merchant image overriding mobile gradient**
- `sections/section-comparison-table.liquid`: removed the mobile-scoped `our_row_bg` override entirely (previously applied to `.ct__cell--solution` and `.ct__col-track .ct__cell` inside `@media max-width:749px`). Mobile now **always** uses the CSS gradient, even if a background image is uploaded — cover-cropping a photo into these small chip shapes doesn't work the way it does for desktop's single wide bar. Desktop's `our_row_bg` override on `.ct__row--ours::before` is untouched (a merchant uploading their own photo is a legitimate feature there, wasn't asked to remove it, and desktop's proportions can actually accommodate a real photo).

**Bug 2 — growing slider-drift glitch (the "thin strip that gets wider")**
- Root cause found by reading `assets/comparison-table.js`: `updateOffset()` measures width from `this.firstViewport` (the **header row's** `.ct__col-viewport`) and applies that *same* pixel offset to every row's track via `translateX`. `.ct__row--ours .ct__col-viewport` had `margin-right: -8px` (added several rounds ago for the Figma bleed), making that one row's viewport 8px wider than the header's. Since the JS assumes uniform width across all rows, each slide advance compounded the 8px mismatch (16px on slide 3, 24px on slide 4) — a growing sliver of the previous slide left visible, worse with each swipe.
- **Fix**: removed `margin-right: -8px` from `.ct__row--ours .ct__col-viewport` in `assets/section-comparison-table.css`. Sacrifices a small decorative right-edge overhang in favor of correct slider math — a functional bug outranks a few px of visual polish.
- **Verified both**: read the actual `comparison-table.js` slider logic (not guessed) to confirm the mechanism, then built a local repro applying the *same* shared `translateX` value to all three rows' tracks (header/ours/competitor) — confirmed content lines up perfectly post-fix with no strip.
- **Reusable lesson**: any `margin`/width override scoped to a single row's `.ct__col-viewport` will break this slider, since the JS assumes all tracks share one row's measured width. Future bleed/decoration on that element needs to go on a child or pseudo-element instead of resizing the viewport itself (same principle as the desktop `::before` bleed fix from a few rounds back).

## 2026-08-29 — Comparison Table: mobile "how it works" gradient was washed out — auto-sized ellipse doesn't match a real cell's proportions

User pasted the client reference `mobile.png` again, saying the color still didn't match — confirmed it actually was that exact file (matches pixel-for-pixel). Built a local headless-Chrome repro using the *current* live CSS (including the align-items:center change) and could directly see the problem: the radial gradient rendered nearly flat/uniform, not matching the reference's visible dark→bright contrast.
- **Cause**: `radial-gradient(ellipse at 50% 100%, ...)` uses relative/auto ellipse sizing (`farthest-corner` by default), which scales the gradient's "reach" based on the *box's own* proportions. The real cell's actual rendered size/aspect ratio doesn't match the tiny Figma mockup (79px/51px tall) the color stops were originally tuned against, so the visible contrast washed out — same underlying class of bug as the earlier `background-size: cover` cropping issue and the `100% 100%` stretch smearing a raster image, just showing up in gradient math instead of image scaling this time.
- Did a denser 2×6 pixel grid sample (not just 9 corner/center points) directly on `.claude/features/feature-comparison-table/reference/mobile.png`'s attribute chip to pin down the true glow position: brightest around x=58-60%, y=90% (slightly right of center, near the bottom), darkest at the top corners.
- **Fix**: switched to a fixed-pixel-size ellipse — `radial-gradient(ellipse 220px 90px at 58% 100%, #89a872 0%, #6b8d5d 45%, #587a4c 75%, #54774c 100%)` in `assets/section-comparison-table.css`. Fixed px radii keep the visible contrast consistent regardless of the cell's actual rendered size, instead of scaling (and washing out) with box proportions.
- Verified the fix visually via the same local repro before reporting back, and confirmed live on the running `theme dev` server.

## 2026-08-29 — Comparison Table: mobile "how it works" chip now centers vertically to match the Solution chip

User confirmed the gradient/color/layout finally matched (the CSS-gradient switch resolved the Theme Editor caching confusion) and asked for two small follow-ups on mobile:
1. Center the "how it works" chip vertically against the Solution chip's height, instead of top-aligning it (which left all the blank space at the bottom).
2. Make sure the white card container behind it is explicitly white.
- **Fix**: `.ct__row--ours .ct__col-track` changed `align-items: flex-start` → `align-items: center` in `assets/section-comparison-table.css`. Added an explicit `background-color: #ffffff` to `.ct__row--ours .ct__col-viewport` (it was already inheriting white from the base rule, but made it explicit per the request).
- Verified both via the running `theme dev` server and a local headless-Chrome repro before reporting back.

## 2026-08-29 — Comparison Table: dropped the bundled background-image assets entirely, switched desktop to a CSS gradient too

After verifying (via a direct fetch of the actual live product page through the theme dev proxy) that the code was rendering correctly, the user's screenshot still showed the stale/broken look — but it turned out to be coming from the **Shopify Admin Theme Editor's own embedded preview iframe**, a separate caching layer from the regular storefront (unaffected by incognito, since the staleness lives on Shopify's side). Rather than keep fighting that cache, the user asked to just drop the image-based background approach entirely and use a CSS gradient for the desktop row too (mobile's chips already used CSS gradients since two rounds ago) — this removes any dependency on a separately-cached PNG asset for the default look.
- `assets/section-comparison-table.css`: `.ct__row--ours::before` (desktop) now uses `background-image: linear-gradient(90deg, #7aa66f 0%, #41643a 100%)` instead of `url('ct-row-bg-desktop.png')`.
- `sections/section-comparison-table.liquid`: the `{% style %}` block's background-image rules (desktop `::before` and the mobile media query) now only fire `{% if section.settings.our_row_bg != blank %}` — i.e. only when a merchant explicitly uploads their own photo. With no upload, the CSS file's own gradient is the only thing painting the background — nothing there to go stale independently of a CSS deploy.
- Also fixed a latent bug while touching this: the mobile merchant-upload override was still targeting `.ct__row--ours .ct__col-viewport` (the full-height *invisible* white container from the restructure two rounds ago) instead of the actual visible chip (`.ct__row--ours .ct__col-track .ct__cell`) — corrected the selector so a merchant's uploaded photo would actually land on the right element.
- **Orphaned assets**: `ct-row-bg-desktop.png`, `ct-row-bg-mobile-solution.png`, `ct-row-bg-mobile-attribute.png` are no longer referenced anywhere except one stale comment. Left in place per the no-delete-without-approval rule — safe to remove whenever convenient.
- **Reusable lesson**: when a fix is confirmed correct via the local `theme dev` proxy and even a direct fetch of the real product page, but the user's Shopify Admin Theme Editor screenshot still shows the old look, suspect the Theme Editor's own embedded preview iframe caching before further code changes — closing/reopening the editor or using its actual "Preview" (opens a real storefront tab) bypasses it.

## 2026-08-29 — Comparison Table: found the real bug behind the mobile "seam + duplicate chip" via a local repro (not the gradient)

User's next screenshot showed a new artifact: a second dark rectangle with its own rounded corners appearing below the "how it works" chip, separated by a lighter seam. Multiple rounds of reasoning about gradient math didn't explain a dark→bright→dark vertical pattern, so instead of guessing again, built a **minimal standalone HTML file** loading the actual `assets/section-comparison-table.css` directly (`file://` URL) with just the relevant markup, and screenshotted it with headless Chrome (`/Applications/Google Chrome.app/Contents/MacOS/Google Chrome --headless=new --window-size=390,900 --screenshot=...`) — no `chromium-cli` or Playwright installed, so used the OS-provided Chrome binary directly. `pip install pillow` had already been used repeatedly this session for pixel sampling; same throwaway-tool pattern here.
- **Root cause found immediately**: a huge extra green block appeared at the bottom of the repro that wasn't in the test markup at all. It was `.ct__row--ours::before` — the pseudo-element added earlier for the *desktop* column-divider-alignment fix (absolutely positioned, `left: -20px; right: -20px`, painting the desktop bleed background). The assumption that `display: contents` on `.ct__row--ours` (mobile turns every row into `display: contents` so its children flow into the shared card grid) would suppress `::before` generation was wrong for this browser — the pseudo-element still rendered, and since `display: contents` doesn't establish a normal containing block, the absolutely-positioned `::before` fell through to some ancestor and rendered as a stray box. This is what looked like a "duplicate chip with a seam" — not a gradient/math issue.
- **Fix**: added `.ct__row--ours::before { display: none; }` inside the mobile media query in `assets/section-comparison-table.css` — mobile already paints its own chip backgrounds directly on the cells, so this pseudo-element should never render there at all.
- **Verified by re-running the same local repro** (screenshot before/after) before touching the live site — confirmed the stray block is gone and the layout matches intent.
- **Takeaway**: when reasoning about a CSS bug analytically stops converging after a couple of rounds, build an isolated local repro and actually look at it (headless Chrome + a bare HTML file loading the real CSS) rather than continuing to guess against the live Shopify page, which this session can't screenshot directly anyway (see earlier `run`-skill note — the section isn't placed in any committed template).

## 2026-08-29 — Comparison Table: mobile "how it works" white card background was going missing below the chip

User reported the white card background below the (now-shorter) "how it works" chip had gone missing, showing the transparent/cream card background instead.
- **Cause**: giving `.ct__row--ours .ct__col-viewport` `align-self: start` (to make the chip shorter, matching Figma) shrank the *entire white container* to content height, not just the chip — so the leftover row space below it (up to Solution's taller height) was no longer covered by anything and fell through to `.ct__card`'s transparent mobile background.
- **Fix**: restructured so the chip and its white backdrop are separate elements. `.ct__row--ours .ct__col-viewport` no longer has `align-self` — it stays full height with its normal white background (inherited from the base rule), so it always covers the same height as Solution's chip, matching every other row. The shrink-to-content + radial-gradient + rounded corners moved down onto `.ct__row--ours .ct__col-track .ct__cell` (the actual visible sliding text cell), with `align-items: flex-start` added to `.ct__row--ours .ct__col-track` so the cell no longer stretches to fill the track. The viewport's `-8px` right bleed stays on the viewport itself (safe from its own `overflow: hidden`, since a negative margin is the element's own box, not a child overflowing it).
- Verified live via the running `theme dev` server.

## 2026-08-29 — Comparison Table: "how it works" mobile chip glow is radial, not linear — found by upscaling the Figma export

User said mobile still didn't match after the previous multi-stop linear-gradient fix, pointing at the Figma node again. Re-fetched the node fresh (confirmed the Figma file itself hadn't changed) and, since precise pixel sampling vs. visual impression seemed to disagree, upscaled `assets/ct-row-bg-mobile-attribute.png` 5x with Lanczos resampling to actually see its shape clearly instead of guessing from coarse samples.
- **Finding**: the glow is genuinely **radial**, centered at the bottom-middle of the chip — brightest there (`#7ca56a`), darkest at both top corners (`#497947`/`#497444`). All previous attempts modeled it as a left-to-right `linear-gradient`, which can't reproduce a corner-vs-center falloff no matter how many stops are added — wrong gradient shape, not just wrong stops.
- Also re-sampled the Solution chip's 9 key points (corners/edges/center) the same way and confirmed *that one* really is a simple horizontal light→lighter fade (no vertical variation) — so it was already correct and untouched.
- **Fix**: `.ct__row--ours .ct__col-viewport` now uses `radial-gradient(ellipse at 50% 100%, #7ca56a 0%, #699660 40%, #4d7c49 75%, #497446 100%)`, sampled directly from the 9-point grid on the actual Figma export. Verified live via the running `theme dev` server.
- **Lesson for next time**: when a gradient still "doesn't match" after getting stop colors right, check the gradient's *shape* (linear vs. radial, axis, center point) before re-tuning colors again — upscaling the source image to actually look at it settled this in one step after several rounds of color-only guessing.

## 2026-08-29 — Comparison Table: mobile chip colors rebuilt from actual sampled reference pixels (not a simple fade)

User flagged the mobile color as still wrong after the align-self fix. Rather than guess again, pixel-sampled `.claude/features/feature-comparison-table/reference/mobile.png` precisely within each chip's actual bounds (located visually via cropping, not blind percentage math). Found the color pattern is **not** a simple left-to-right fade:
- **Solution chip**: lightens left→right (`#7e9c69` at its left edge → `#9eb582` at its right edge, near the seam) — opposite direction from what was implemented (`#7aa66f 0% → #41643a 100%`, a fade to *dark*).
- **Attribute ("how it works") chip**: an off-center glow — dark at both edges (`#56794d` left, `#5d7e50` right) with a brightness peak around 70-80% of its width (`#78946a`), not a monotonic fade.
- This non-monotonic "glow" shape matches the soft radial highlight found baked into the raw Figma image assets in an earlier round (`ct-row-bg-mobile-attribute.png`) — likely a genuine texture/lighting effect in the source design, not a Figma scaling artifact as I'd assumed when reverting to a flat 2-stop gradient.
- **Fix**: rebuilt both mobile gradients as multi-stop `linear-gradient`s using the directly-sampled hex values (solution: 2-stop lightening; attribute: 4-stop dark→bright→dark) in `assets/section-comparison-table.css`. Still plain CSS (not an image) so it scales cleanly to any cell height — only the color *shape* changed, not the image-vs-gradient approach from the previous fix.
- Verified live via the running `theme dev` server.

## 2026-08-29 — Comparison Table: mobile chips were stretched to a shared height, Figma sizes them independently

User's next screenshot vs. the Figma mobile reference: both Terra Therapy chips were stretched to the same tall height (matching the taller Solution icon+logo box), while Figma shows the "How it works" chip ending well above the Solution chip — sized to just its own 2-line text.
- **Cause**: CSS Grid default `align-items: stretch` — since `.ct__cell--solution` and `.ct__col-viewport` are direct items in the shared `.ct__card` grid (rows dissolve via `display: contents`), Grid stretches every item in a row to match that row's tallest item.
- **Fix**: added `align-self: start` to both `.ct__row--ours .ct__cell--solution` and `.ct__row--ours .ct__col-viewport` in `assets/section-comparison-table.css` — each chip now sizes to its own content instead of being forced to match its sibling, leaving the same blank space below the shorter chip that Figma shows. Verified live via the running `theme dev` server.

## 2026-08-29 — Comparison Table: mobile "How it works" chip gradient wasn't sweeping full width

User's next screenshot showed the gradient-revert fix rendering cleanly (no more blob) but the "How it works" chip still looked almost flat/uniform dark green instead of matching Figma's visible left-to-right sweep. Cause: `.ct__row--ours .ct__col-viewport`'s gradient stop was `#41643a 30%` (a leftover from the earlier image-based attempt, where a short stop made sense as a subtle seam blend) — that made ~70% of the chip flat solid color. Figma's mobile mockup actually shows the gradient sweeping across the chip's *entire* width, same as the Solution chip. Changed the stop from `30%` to `100%` to match. Verified live via the running `theme dev` server.

## 2026-08-29 — Comparison Table: mobile Terra Therapy chips reverted to CSS gradient (image approach broke on mobile)

User sent a screenshot showing the mobile background images looked wrong — both the Solution chip and "How it works" chip showed an ugly glowing blob instead of a clean gradient.
- **Root cause**: zoomed into the actual saved crop files (`ct-row-bg-mobile-solution.png`, `ct-row-bg-mobile-attribute.png`) and found Figma's mobile chip graphic isn't a clean 2-stop linear gradient like the desktop one — it's a small soft radial highlight/glow texture, sized for Figma's tight ~79px/51px-tall mockup cell. The live theme's actual cell is much taller (real text wraps to 3-4 lines), so stretching that small glow via `background-size: 100% 100%` smeared it into the blotch the user saw. This isn't fixable by swapping `background-size` modes — `cover` would just zoom into a near-solid slice of the same blob; the box proportions are too different from Figma's mockup for any raster-image approach to hold up.
- **Fix**: reverted the two mobile cells (`.ct__row--ours .ct__cell--solution`, `.ct__row--ours .ct__col-viewport`) in `assets/section-comparison-table.css` back to a plain `linear-gradient(90deg, #7aa66f 0%, #41643a ...)`, using the same two colors already sampled from the reference PNGs in an earlier round. A flat gradient scales cleanly to any cell height with no artifacts, and reads as visually equivalent to Figma's intent. **Desktop keeps the real image** — its box proportions are close enough to the Figma export that it renders correctly (user-confirmed).
- Updated the Liquid `{% style %}` block so the mobile media query only sets a `background-image` (with `cover`) when the merchant uploads their own `our_row_bg` photo — otherwise mobile falls through to the CSS gradient default.
- **Leftover**: `assets/ct-row-bg-mobile-solution.png` and `assets/ct-row-bg-mobile-attribute.png` are no longer referenced anywhere. Left them in place rather than deleting without approval (per the no-delete-without-approval rule) — safe to remove whenever convenient.
- **Verified via the running `shopify theme dev` server** (port 9292) that the compiled CSS reflects this change.

## 2026-08-29 — Comparison Table: fixed desktop column-divider misalignment on the Terra Therapy row

User sent a screenshot showing the background-image fix from the previous entry actually worked (confirmed the gradient graphic renders correctly), but the row's internal vertical divider lines (between Solution/How it works/etc.) didn't line up with the header row's dividers above it.
- **Root cause**: `.ct__row--ours` used `margin: 0 -20px` to bleed 20px past the white card on each side. Every row shares the same `.ct__row { grid-template-columns: 190px repeat(4, 1fr); }` definition, but that -20px margin makes *this row's own box* 40px wider than sibling rows — so the fixed `190px` Solution column lands correctly, but the `1fr` tracks after it compute to different absolute pixel positions than in the header/competitor rows, which stay at the card's normal width. Hence the visible divider misalignment.
- **Fix**: stopped resizing the row itself. `.ct__row--ours` no longer has any margin/width override — it's exactly the same box size as every sibling row, so its grid (and thus its dividers) line up perfectly. The ~20px bleed + background image is now painted on a `.ct__row--ours::before` pseudo-element (`position: absolute; left: -20px; right: -20px`) sitting behind the row's content. Moved the Liquid `{% style %}` block's background-image rule from `.ct__row--ours` to `.ct__row--ours::before` to match.
- **Confirmed live**: this session has an active `shopify theme dev --store developmet-test.myshopify.com` process (port 9292) — verified the compiled CSS served at `/cdn/shop/t/1/assets/section-comparison-table.css` already reflects the new `::before` rule.
- **Note**: this section isn't placed in any committed template/JSON in this repo — it's only reachable via a live Theme Editor/customizer session. Screenshots from the user are the only way to visually verify changes; can't fetch the rendered page directly.

## 2026-08-29 — Comparison Table: fixed background-image cropping on desktop (cover → stretch)

User reported the desktop Terra Therapy row still didn't match Figma after the previous change. Root cause: `background-size: cover` preserves aspect ratio and crops to fill — since the live row's actual width:height ratio won't exactly match the Figma-exported `ct-row-bg-desktop.png` (1313×116, sized off Figma's fixed 1440px canvas), it was cropping part of the gradient off the edges (likely losing some of the light-green start and/or dark-green end), leaving a narrower/duller-looking band than intended.
- **Fix**: changed `background-size: cover` → `background-size: 100% 100%` (+ `background-repeat: no-repeat`) on `.ct__row--ours` (desktop) and both mobile split cells (`.ct__cell--solution`, `.ct__col-viewport`). Since the image is a smooth two-tone gradient with no shape/detail to protect, non-uniform stretching is visually safe and guarantees the gradient's start/end always land exactly at the row's edges regardless of its actual rendered size.
- **Watch for**: if a merchant uploads their own `our_row_bg` with real photographic content (not a smooth gradient), this same `100% 100%` stretch would visibly distort it. Worth reconsidering (e.g. `cover` + wider export margins) if that setting sees real use.

**Not yet verified in a live theme preview** — no dev server available in this session.

## 2026-08-29 — Comparison Table: Terra Therapy row now uses Figma's actual background image; default competitor icons added

User asked for the Terra Therapy row to use an **image** background (matching Figma nodes 1-2405/1-4012), not a computed CSS gradient, plus a default icon for the competitor "Icon / product image" setting.
- Downloaded the actual rendered graphics via the Figma MCP (`download_assets`, not screenshots) for: the desktop row bar (node 1:2416, 1313×116), the mobile attribute chip (node 1:4021, 246×51), and the mobile solution chip (node 1:4034, 120×79). Saved as `assets/ct-row-bg-desktop.png`, `assets/ct-row-bg-mobile-attribute.png`, `assets/ct-row-bg-mobile-solution.png`.
- Wired these into `sections/section-comparison-table.liquid`'s `{% style %}` block as the default `background-image`, with the existing `our_row_bg` merchant-upload setting still taking priority when set (`{% if %}` inside the `url(...)`). `assets/section-comparison-table.css` now just sets `background-color: #41643a` (solid fallback) + `background-size: cover` on `.ct__row--ours` and its mobile-split cells, instead of a `linear-gradient(...)`.
- **Found and fixed a stale bug while in there**: `assets/ct-icon-pain-meds.png` (from an earlier round) was a completely blank/transparent 1024×1024 PNG — re-downloaded the real "Pain Meds" bottle photo from Figma node 1:2455 to replace it.
- Added a default-icon fallback in the competitor block markup: when `block.settings.icon` is blank, a `{% case block.settings.name %}` matches the 4 shipped presets (Sleeping Pills / Pain Meds / Chiropractor / Supplements) to their existing `ct-icon-*.png` assets; any other/custom competitor name still falls back to the empty placeholder div (a merchant adding a new competitor is expected to upload their own icon — no generic catch-all icon was introduced).
- **Trade-off to flag**: the icon fallback matches on the block's exact `name` text. If a merchant renames one of the 4 preset blocks (e.g. "Sleeping Pills" → "Ambien"), it loses its default icon and falls back to the empty placeholder. Kept simple per the "CMS settings, not pixel-perfect" philosophy — not worth a more robust keyed-lookup for 4 presets.

**Not yet verified in a live theme preview** — no dev server available in this session.

## 2026-08-29 — Comparison Table: Terra Therapy row was too dark (gradient end color fixed)

User flagged the row as "too dark" vs. `.claude/features/feature-comparison-table/reference/desktop.png` / `mobile.png`. Rather than eyeball it, sampled actual pixel RGB values from those reference PNGs (Python/Pillow, temporarily installed then removed). Result: the row's dark end is a **dark forest green** (~`#41643a`, sampled consistently at the row's right edge on desktop), not the near-black `#31331e` (the theme's primary text/brand color) that had been reused for it since Round 2.
- Replaced `#31331e` with `#41643a` as the gradient end-color / background-color fallback in all 3 places: desktop `.ct__row--ours`, and mobile `.ct__row--ours .ct__cell--solution` / `.ct__row--ours .ct__col-viewport`.
- Also adjusted the desktop gradient stop from `60%` to `100%` — sampled data showed the color still gradually darkening past the 60% mark, not plateauing there.
- **Note for future rounds**: `#31331e` is the theme's brand/text color and looks tempting to reuse for "dark olive," but the actual design wants a distinct, greener dark tone (`#41643a`) for this specific gradient — don't default back to `#31331e` here.

**Not yet verified in a live theme preview** — no dev server available in this session.

## 2026-08-29 — Comparison Table: Terra Therapy row mobile chip fix (matched against live Figma node data)

Compared `assets/section-comparison-table.css` against the actual Figma layer data (via `get_design_context`, not just screenshots) for node 1-2405 (desktop) and 1-4012 (mobile):
- **Desktop**: Figma's highlight is one continuous rounded bar (single 5px radius, ~20px bleed past the card each side, single green→dark gradient) spanning all 5 columns. Current CSS (`margin: 0 -20px`, `border-radius: 5px`, `linear-gradient(90deg, #7aa66f 0%, #31331e 60%)`) already matches this — left unchanged.
- **Mobile**: Figma shows the Solution cell and "How it works" cell as two *independently* fully-rounded floating chips (both corners rounded on each, not a shared flat seam), both pulling from the same gradient texture. The shipped CSS had a bug: it rounded only the outer corners of each cell (`5px 0 0 5px` / `0 5px 5px 0`) to force a flush square seam in the middle, and the viewport cell used a flat `#31331e` instead of a gradient. Fixed both cells to `border-radius: 5px` (all corners) and gave the viewport cell a short gradient (`linear-gradient(90deg, #7aa66f 0%, #31331e 30%)`) reusing the existing sage-green/olive tokens instead of introducing new colors.

**Not yet verified in a live theme preview** — no dev server available in this session. Visual confirmation is pending the user's next look/QA round.

## 2026-08-29 — Comparison Table: Terra Therapy row visual fix (Round 2 QA feedback)

Fixed the highlighted "Terra Therapy" row in `sections/section-comparison-table.liquid` / `assets/section-comparison-table.css` to match the client's updated reference (`.claude/features/feature-comparison-table/reference/expected-result.png`):
- Background: flat `#31331e` → `linear-gradient(135deg, #7aa66f 0%, #31331e 100%)`, reusing existing theme tokens rather than inventing new colors.
- Row shape: now inset with `margin: 8px` + `border-radius: 12px` (floating rounded "chip" look) instead of flush/square against the card edges.
- Icon: discovered `ct-product-photo.png` is actually the small wing/fabric icon shown in the mockup, not a distinct square photo — the old CSS was cropping it into an 80×80 box and rotating it 17°/1.4×, producing an unrecognizable grey blob. Now rendered at natural aspect ratio via `object-fit: contain`, ~90px wide (scaled down at tablet/mobile).

**Not yet verified in a live theme preview** — no dev server available in this session; visual confirmation is pending the next QA round. See Round 3 in `OUTPUT-qa-debugging.md`.

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# OUTPUT-project-log.md — v1.0

# AI Shopify Developer Bootcamp

# by Coding with Jan

# https://codingwithjan.com

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
